'use strict';

let asmd = (function() {
  function asmd() {

  }

  function add(a, b) {

    let maxLength = Math.max(a.length, b.length);

    let result = [];

    let memory;
    for (let i = maxLength-1, j = 2; i >= 0; i--) {
      let sum = a;
      result.push();
    }
  }

  return {
    add: function() {
      return 1;
    },
  };
}());

// TODO JsDOC
