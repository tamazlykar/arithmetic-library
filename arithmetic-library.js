"use strict";

var asmd = (function() {
  function asmd() {

  }

  function add(a, b) {

    let maxLength = Math.max(a.length, b.length);

    let result = []

    let memory
    for (let i = maxLength-1, let j = ; i >= 0; i--) {
      let sum = a
      result.push()
    }
  }

  return {
    hw: function() {
      console.log("Hello world");
    }
  }
}());

//TODO JsDOC
